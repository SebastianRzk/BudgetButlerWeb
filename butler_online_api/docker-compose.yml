
services:
  db:
    image: mariadb
    restart: always
    environment:
      MARIADB_ROOT_PASSWORD: example
      MYSQL_PASSWORD: butler
      MYSQL_DATABASE: butler
      MYSQL_USER: butler
    ports:
      - 3306:3306
