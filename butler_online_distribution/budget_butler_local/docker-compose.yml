version: '3'
services:
  budget-butler-static:
    image: budget-butler-static
    ports:
      - "80:80"
    env_file:
      - db.env
    restart: unless-stopped
  budget-butler-fpm:
    image: budget-butler-fpm
    env_file:
      - db.env
    restart: unless-stopped

  db:
    image: mariadb
    volumes:
      - db:/var/lib/mysql
    env_file:
      - db.env
    restart: unless-stopped


volumes:
  db:

